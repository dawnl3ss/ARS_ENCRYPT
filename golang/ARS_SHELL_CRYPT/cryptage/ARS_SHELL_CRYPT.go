package cryptage

import "github.com/Neptune-Dev/ARS_SHELL_CRYPT"

const (
	METHOD_CRYPT   = 0
	METHOD_DECRYPT = 1
)

func count() int {
	return len("")
}

func charToAchar(fChar string, method int, spacing int) {
	nChar := ""
	
	if method == METHOD_CRYPT {
		for i := 0; i <= count() - 1; i++ {
			if standar
		}
	}

	return nChar
}